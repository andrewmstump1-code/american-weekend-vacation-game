# american-weekend-vacation-game
a 2D side-scrolling game for my software engineering class

## Local setup

1. Install dependencies:
   `npm install`
2. Set `NEON_DB_URL` in your deployment environment.
3. Deploy to Vercel so `/api/subscribe` can store landing page emails in Neon.
